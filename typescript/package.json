{
    "name": "sn-export-compile",
    "version": "1.0.0",
    "description": "JS Exporter Compiler",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "watch:dev": "NODE_OPTIONS=--openssl-legacy-provider npx webpack --mode=development --watch",
        "watch": "NODE_OPTIONS=--openssl-legacy-provider npx webpack --mode=production --watch"
    },
    "author": "Supernova.io",
    "license": "MIT",
    "dependencies": {},
    "devDependencies": {
        "ts-loader": "^7.0.5",
        "typescript": "5.2.2",
        "webpack": "^5.91.0",
        "webpack-cli": "^5.1.4"
    }
}
